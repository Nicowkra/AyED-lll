#include <iostream>
#include <vector>
#include <string>
#include <cmath>
using namespace std;

int main(){
    int i = 0;
    int casos;
    cin >> casos;
    while (casos>i){
        i++;
        int n, m;
        cin >> n >> m;
        vector<vector<int>> grid(n, vector<int>(m));
    }
}